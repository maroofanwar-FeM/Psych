[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Client-side routing (react-router-dom) - let index.html handle all paths
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
