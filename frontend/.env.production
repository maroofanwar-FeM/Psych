# Left blank on purpose: requests go to the same origin (relative /api/... paths),
# and netlify.toml proxies /api/* to the Render backend. This keeps frontend and
# backend looking like one origin to the browser, avoiding cross-site cookie issues.
VITE_API_BASE_URL=
